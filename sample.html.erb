<!DOCTYPE html>
<html>
<head>
  <title>chattr widget - Rails sample</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
</head>

<body>
  <!-- Set your API key in: ENV["CHATTR_API_KEY"] -->
  <script type="text/javascript" src="/api/chat.js?key=<%= ENV["CHATTR_API_KEY"] %>"></script>
  <script>
    // Let's identify your current user (defined as current_user) in chattr!
    _chattr.identify('<%= current_user.firstname %>', {
      user_id: <%= current_user.id %>,
      email: '<%= current_user.email %>',
      plan: '<%= current_user.plan %>'
    });
    
    // Initialize the widget AFTER identifying your current user
    _chattr.init();
  </script>
</body>
</html>
